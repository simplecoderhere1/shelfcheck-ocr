server/venv/
server/__pycache__/
server/*.pyc
server/.env
*.DS_Store

# Run artifacts
backend.err
backend.log
*.log
*.pid
test_env/
__pycache__/
*.pyc
.env

# Local-only fp32 recognition model kept for A/B benchmarking
# (the shipped rec.onnx is the MatMul-int8 build; fp32 is in git history)
models/rec.fp32.onnx

# Modal: keep the fp32 rec model out of git (large; lives locally + in history)
modal_server/**/rec.fp32.onnx

# Staged copies for the HF Space (assembled by hf_space/prepare_space.sh)
hf_space/models/
hf_space/ocr_pipeline.py

# Azure AI Vision key (local only — never commit; production home is a Worker secret)
.azure_key*

# Azure Functions deps (installed by the SWA build)
api/node_modules/

# wrangler local state (miniflare DO storage, build cache)
workers/.wrangler/
